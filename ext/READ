2026-07-19
- Workspace delayed startup replay now passes `TermControl`/`ICoreState` by value through the host/extension seam and re-checks live runtime state plus connection status after each `co_await`, fixing the portable startup crash where SSH workspace restore could call `SendInput` through a dangling reference after the connect callback returned.
- Workspace persistence no longer reads or writes legacy `order` metadata inside `tab.yaml`; node identity/display continue to come from the tab directory name, visible tab order stays in workspace-level `tabOrder`, and node load fallback is now deterministic by tab directory name instead of per-tab order fields.
- Workspace list order now persists at `%USERPROFILE%\.wt\workspaces\order.yaml` as the ordered workspace-name sequence instead of numeric `workspace.yaml.order`, and save-time workspace/tab renames now rewrite the persisted order names so root order plus per-workspace `tabOrder` stay aligned with sanitized folder names.
- Workspace persistence now keeps live tab drag order in workspace-level `tabOrder` metadata instead of rewriting node definitions, saves each node's `profileName` alongside `profileGuid`, and stops inventing fallback startup directories while defaulting unresolved OS inference to `linux`.
- Workspace restore/startup now validates each visible tab before loading it: nodes with missing profiles are dropped from pending startup state and startup action generation instead of falling back to an unrelated default profile and risking a restore-time crash.

2026-07-14
- Workspace chat 的本地缓存、草稿持久化、terminal 事件落盘现在编译自 `ext\src\core\chat`；`glue\chat` 只保留宿主耦合控制器和兼容头，core/chat 源在 `TerminalAppLib` / `WorkspaceExtension` 项目里都改为不依赖宿主 PCH。
- Workspace chat 的 terminal input 解析、文本裁剪/摘要/交互命令判定、workspace/draft/state key 规则现在也编译自 `ext\src\core\chat`；`glue\chat` 对应头已退化为兼容 include。
- Workspace chat 的 terminal input capture 编排（pending input 消费、归一化、多行拆分、buffer snapshot 首次采样、working-directory 同步、逐行 snapshot 生成）现在也收进 `ext\src\core\chat\WorkspaceChatStateHelpers.*`；`WorkspaceChatTerminalGlue.cpp` 只保留宿主 `TermControl` 读取、diagnostic 记录和 controller 回写。
- Workspace launch/runtime 的 profile/commandline metadata 推断、SSH transport/`-t` 判定、runtime launch state 归并、node launch property 优先级决策、tracked working-directory 选择、以及 workspace dirty 判定现在都通过 `ext\src\core\workspace` 暴露纯 helper；`WorkspaceLaunchRuntimeGlue.cpp` / `WorkspaceSaveRuntimeGlue.cpp` / `WorkspaceChatTerminalGlue.cpp` 只保留宿主对象探测、live tab 遍历和 UI/controller 编排。
- `WorkspaceHostBridge.h` 现在集中承接持久化 manager/state 的 load/save 模板、当前 workspace definition/node 的 load-resolve 入口，以及 window-state / node-order / lock-state 的持久化封装；散落在 glue 里的重复 `Load()+Save()` 事务进一步收口到了 bridge。
- Workspace chat controller 现在也编译自 `ext\src\core\chat`，负责消息 id / correlation id / timestamp 生成以及 chat/event append 编排；glue 只保留 diagnostic hook，把 `chat_controller_submit` JSON 记录挂回宿主日志层。
- Workspace save/rename 相关的纯规则 helper（workspace 颜色规范化与默认色挑选、持久化命名唯一化、workspace/node 名归一化、save target 解析、默认保存名建议）现在通过 `ext\src\core\workspace` 实现并由 glue facade 暴露，`WorkspaceSaverGlue.cpp` / `WorkspaceTerminalGlue.cpp` / `WorkspaceEditorGlue.cpp` / `WorkspaceSaveRuntimeGlue.cpp` 已改为调用这些 core helper。
- Workspace layout 比较规则和 workspace window-state upsert/remove 规则现在也通过 `ext\src\core\workspace` 实现并由 glue facade 调用；`_CurrentWorkspaceNeedsSave()` 与 `RefreshWorkspaceWindowState()` 已不再自己在 glue 里手写这些纯规则。
- 保存时“captured workspace 如何并入持久化 workspace 列表”的编排现在也通过 `ext\src\core\workspace::PrepareWorkspaceForSave(...)` 实现；`WorkspaceSaverGlue.cpp` 只负责捕获 live tab/runtime 数据、保存 manager、回绑 node id、以及宿主 UI/dispatcher 更新。
- Workspace diagnostics now record explicit exception payloads for workspace save failures, key AppLogic catch paths, and the XAML application's unhandled-exception event into `%USERPROFILE%\.wt\logs\workspace-chat-diagnostics.jsonl` instead of relying only on `LOG_CAUGHT_EXCEPTION()`.
- Workspace persistence now uses `%USERPROFILE%\.wt\workspaces` as the primary storage root for workspace definitions and per-workspace state, loads any legacy root-level `%USERPROFILE%\.wt\<workspace>` directories for migration, and deletes those legacy root workspace directories/state files after saving.
- The portable single-file packer now writes the final `WindowsTerminalPortableGeekEdition_System...` artifact directly into `bin` and deletes legacy `WindowsTerminalDev...` / old alias outputs instead of first emitting a package-name exe and then copying it.
- Locking the current workspace now directly removes any open settings tab and workspace management tab before the locked UI state is applied, preventing either editable page from staying alive in a conflicting state.
- Build target names are now reduced to just `ext` and `full`: bare `cmake --build .\build` defaults to the Release `ext` ALL target, while `cmake --build .\build --target full` is the explicit full-chain rebuild path.
- The portable repack path now caches a baseline prepared distribution zip under `microsoft\tmp\portable-package-cache`; once that cache is primed, `ext` rebuilds update only the `WorkspaceExtension.dll/.pri` entries inside the cached zip before rebuilding the final single-file launcher, instead of re-extracting the MSIX/XAML packages, re-running PRI merge, and recreating the whole distribution zip from scratch.
- Measured ext-only incremental timings after the repack cache change: cache-prime run was about compile `4s`, link `2s`, repack `19s`, total `28.95s`; warm-cache runs dropped to compile `4s`, link `2s`, repack `2s`, total `11.90s` and `12.12s`.
- Added `docs\design\workspace-terminalpagebase-plugin.md`, introduced a `TerminalPageBase` host interface, switched `TerminalPage` to runtime `LoadLibrary` / `GetProcAddress` loading of `WorkspaceExtension.dll`, moved `TerminalPageWorkspaceGlue.cpp` back to the host build through `WorkspaceTerminalPageLegacyGlue.cpp`, and removed `TerminalApp`'s static `WorkspaceExtension.lib` link so the DLL is now a real dynamic plugin boundary.
- Added `ext\src\workspace\dll\WorkspaceModelWrapper.cpp` so the plugin can build the shared workspace model with its own PCH, disabled extension-side WPO/LTCG to avoid the linker ICE after moving workspace model code into the DLL, and kept the final full portable build producing the `bin\WindowsTerminalPortableGeekEdition_System_0.0.1.0_x64.exe` artifact.
- The portable CMake entry now exposes two build flows: default `ext` rebuilds only the ext workspace/chat runtime through the standard CMake `WorkspaceExtension` target and then repacks from the latest existing MSIX while overlaying the fresh `WorkspaceExtension.dll`/`.pri`, while explicit `full` stays available for changes that touch the older `microsoft\src\...` projects.
- The `build` directory is now configured with the `Ninja Multi-Config` generator for portable work, so bare `cmake --build .\build` uses the default Release `ext` route with Ninja's incremental/cache behavior instead of a Visual Studio-generated `vcxproj` backend.
- The ext-only CMake path now prints timestamped `START` / `END` lines with elapsed seconds for each hot-path stage (`vcpkg`, `package-tool`, `ext-repack`) and for each `WorkspaceExtension` compile/link invocation, so incremental bottlenecks can be read directly from normal Ninja build output.
- The ext-only `WorkspaceExtension` link path now keeps `/GL` + `/LTCG:incremental` for codegen but drops the ext-only link debug-output generation, cutting the latest pure incremental portable run to about compile `22s`, link `45s`, repack `18s`, total `85.63s`.
- The current portable repack now only publishes `WindowsTerminalPortableGeekEdition_System_<version>_<arch>.exe`, and the latest retry after stripping `WorkspaceExtension` compile debug info measured about compile `21s`, link `57s`, repack `18s`, total `97s`.
- Added `ext\src\workspace\dll\WorkspaceExtension.vcxproj` so workspace/chat ext implementation now builds as a dedicated DLL, `TerminalAppLib.vcxproj` no longer compiles the ext implementation `.cpp` files directly, and `TerminalApp.vcxproj` now pulls the ext DLL into the app/package graph.
- The last non-UI workspace/chat timers and saver interaction flags were moved behind `WorkspaceTerminalPageExtension`, and the now-empty `TerminalPageWorkspaceStartupMembers.h` / `TerminalPageWorkspaceMembers.h` shards were deleted; `TerminalPage` now only keeps the remaining page-level workspace UI references that still act as host wiring.
- The workspace extension now owns current-workspace identity/baseline state (`currentWorkspaceId`, `lastWorkspaceId`, save baseline) plus the startup workspace id, so workspace launch/save/runtime glue no longer needs those state fields to live directly on `TerminalPage`.
- The workspace extension now also owns workspace-name edit state plus the workspace editor session state (`WorkspaceManager`, selected index, nav selection, edit mode, dirty flag); the remaining editor glue now resolves those values through the extension boundary instead of storing them directly on `TerminalPage`.
- The workspace extension now owns the pending workspace-node startup-action / skip-next-sendinput state plus the current-workspace lock rule; `TerminalPage` now asks the extension to track startup SendInput suppression and lock status instead of storing those two workspace-specific decisions itself.
- Architecture correction: ext should contain only workspace business logic, state, and interfaces, and should not retain original-project implementation content; native code keeps all workspace UI/interaction ownership while `WorkspaceModel` / `WorkspaceApi` shrink back to stable data/interface layers.
- Architecture correction v2: ext now targets a two-layer split — a pure C++ core/interface DLL plus a separate Terminal-coupled glue layer. The glue layer may participate in `full`, but workspace business logic must move into the pure core layer instead of drifting back into old host/main-branch logic.
- `ext\src` is now physically split by layer: `ext\src\core` holds pure core code, while `ext\src\glue` holds Terminal-coupled chat/workspace/portable glue and packaging code. The ext-only and full build paths were updated to compile from the new glue paths while core workspace sources moved under `ext\src\core\workspace`.
- The workspace extension boundary now also owns the remaining `TerminalPage` startup/close/split/pane-create workspace hooks: startup-state prepare/clear, startup-action skip decisions, close-window save confirmation, split blocking for workspace-managed tabs, and new terminal runtime-state registration all forward through `IWorkspaceTerminalPageExtension` / `IWorkspaceTerminalPageHost`.
- Added `ext\src\workspace\WorkspaceHostInterfaces.h` plus a `WorkspaceTerminalPageHostAdapter` / `WorkspaceTerminalPageExtension` boundary inside `WorkspaceTerminalPageExtension.cpp`; `TerminalPage` now registers the workspace extension once and forwards create, terminal-content resize, settings reload, connection-state startup replay, workspace input-panel changes, and workspace tab-drag gating through that host/extension seam.
- Workspace/chat `TerminalPage` bridge code now builds through `ext\src\workspace\WorkspaceTerminalPageExtension.cpp` as an independent `TerminalAppLib` compilation unit; `TerminalPage.cpp` no longer text-includes `TerminalPageWorkspacePrelude.cpp` or `TerminalPageWorkspaceGlue.cpp`, so ext-side workspace/chat edits stop recompiling that large forked cpp by default.
- Saved workspace chat artifacts now live under `%USERPROFILE%\.wt\workspaces\<workspace-name>\<tab-name>\...`; only unsaved-window artifacts stay under `.wt\cache`, so saved workspace names no longer compete with root-level `.wt` entries like `logs`.
- Workspace chat artifacts no longer use workspace-root `chat` / `drafts` / `terminal` folders; chat history, draft state, and terminal event logs now write under each tab-name directory inside the workspace, and the old root-level compatibility migration was removed.
- Locked workspaces now hide both the tab-row save button and the workspace-flyout save item, and the save click paths also early-return while locked so save can no longer be triggered through stale UI state.
- Saving a live workspace now rebinds each current tab's runtime workspace node id to the just-persisted visible node order, so after closing/reordering tabs the next save no longer reuses metadata from a previously closed tab.
- Workspace chat submit now prepends one leading `\n` as well as the existing trailing `\n` inside the pasted body when `BracketedPasteEnabled` is on, before the separate `SendInput("\r")` submit; non-bracketed two-phase paths keep the old body shape.
- Workspace chat submit now only keeps the two-phase `PasteText(...)+SendInput("\r")` path for bracketed-paste / special-submit terminals; non-bracketed terminals go back to one-shot `SendInput(text + "\r")`.
- Workspace chat submit default paste path now appends `\n` to the pasted body before the separate `SendInput("\r")` submit, so this repro variant sends `PasteText(body + "\n")` followed by one extra submit CR.
- Workspace chat submit now goes back to the original two-phase path for the default transport: it pastes the message body with `PasteText(...)` and then sends a separate `SendInput("\r")` submit, instead of combining body and trailing CR into one `SendInput` payload.
- `TerminalPage.cpp` 中此前被整体挪到 `ext\src\workspace` 的通用 terminal/window/tab/pane/settings/theme/suggestions/new-tab-flyout/lifecycle/drag-event 逻辑已全部移回原文件；`ext\src\workspace` 现在只保留 workspace/chat 相关 glue，`TerminalPageWorkspaceGlue.cpp` 也只再聚合这些 workspace/chat 入口。
- 已删除不再需要的非 workspace glue 文件：`TerminalPageNewTabFlyoutGlue.cpp`、`TerminalPageInteractionGlue.cpp`、`TerminalPageControlGlue.cpp`、`TerminalPageWindowStateGlue.cpp`、`TerminalPageSettingsUiGlue.cpp`、`TerminalPageHelpersGlue.cpp`、`TerminalPageTeachingTipGlue.cpp`、`TerminalPageWindowActionsGlue.cpp`、`TerminalPageThemeRuntimeGlue.cpp`、`TerminalPageSuggestionsGlue.cpp`、`WorkspaceLifecycleGlue.cpp`、`WorkspaceTerminalEventsGlue.cpp`、`WorkspaceTabDragGlue.cpp`。

2026-06-30
- Workspace drag and keyboard tab reorder now auto-save the reordered node positions for saved workspaces, keep existing node definitions unchanged, and no longer surface a manual save affordance for that path.
- Switching to a tab with the workspace input panel now focuses the input box instead of the terminal content.
- Workspace nodes gained a per-node title lock switch, defaulting to fixed node-name tab titles and blocking runtime title rewrites.
- Workspace input submit now preserves embedded Enter/newline content and sends the final execution Enter outside the pasted body so CLI tools actually run the message.
- Portable single-file launcher now embeds `microsoft\res\terminal.ico`, so the `bin` portable exe shows the Windows Terminal icon in Explorer.
- Workspace launch/save now prefer live profile metadata over stale node shellType/OS, so local tabs do not get reopened as SSH just because an old workspace file said `ssh`.
- Workspace title-menu flyout is now kept alive across its open/close lifecycle and restores tab focus explicitly, avoiding the top-left workspace popup crash.
2026-07-01
- Workspace tab close and workspace manager node delete now remove the same saved workspace node entry, so live tabs, manager lists, and reopened workspace order stay in sync.
- Deleting a workspace or deleting its last node now resolves to the same outcome after save: the workspace definition is removed instead of leaving an empty workspace, and the currently opened workspace window is closed.
- Workspace bottom input now keeps Enter for newline, uses Ctrl+Enter to submit, and always sends the full message plus trailing Enter in one terminal `SendInput`; the `ssh -t` fix stays on startup replay instead of changing normal submit behavior.
- Workspace delete actions now persist immediately, and top-tab close falls back to the resolved workspace node id when live runtime metadata is missing, so deleting the current workspace or its last tab actually removes the saved workspace.
- Workspace title-bar save button visibility now matches the workspace menu save action, so both entry points appear under the same unsaved-workspace condition.
- Closing the whole window or leaving the current workspace now prompts to save unsaved workspace changes first, reusing the same save target logic and asking for a name when the workspace has not been saved before.
- The unsaved-workspace exit prompt now explicitly loads its dialog controls before configuring them, avoiding the crash path when the close button opens that prompt for the first time.
- Workspace nodes now persist a per-tab color, default to a profile-guid-derived color, reopen with that color applied, and auto-generate distinct saved colors for repeated uses of the same profile.
- Workspace nodes now persist a show/hide flag, default visible, skip hidden tabs when reopening a workspace, and preserve hidden nodes in saved definitions instead of dropping them from the workspace file.
- Startup-time workspace restore now skips the original queued SendInput for SSH nodes after converting it into deferred post-connect input, so node startup directory and startup command are no longer replayed twice during workspace reopen.
- SSH workspace startup replay now waits briefly after connect, then replays deferred startup in SSH-aware chunks: startup directory restore runs as its own command, while multiline `startupAction` bodies are sent as one block, avoiding `ssh -t` PowerShell `>>` continuations being torn apart by per-line replay; `shellType: pwsh` is also recognized as PowerShell for SSH directory restore.
- Workspace startupAction now round-trips as a real multiline field in `workspaces.yaml`, and workspace save prefers the last multiline logical input over only the trailing line, so SSH `-t` PowerShell `>>` continuations reopen with the full command body intact.
- Workspace save and drag-reorder now keep preserved node metadata matched by node id instead of tab position, so swapping tabs no longer overwrites connectionRef or other retained node data onto the wrong workspace entry.
- Workspace top-tab close and other node lookups now reuse workspace runtime metadata from any pane in the tab instead of only the currently focused pane, so split tabs still remove and persist the correct workspace node by id.
2026-07-02
- Workspace save now refreshes runtime-derived node metadata even for already-saved workspace nodes, and the top save button is recomputed immediately when captured cwd/startup command changes, so the save affordance tracks dirty/not-dirty state instead of lagging behind until another UI refresh.
2026-07-07
- Workspace definitions under `%USERPROFILE%\.wt` now persist as `workspace-name\tab-name` directories with per-folder `workspace.yaml` / `tab.yaml`, so editing or deleting a single saved tab only touches its own folder instead of rewriting one giant file.
- The top-left workspace save button now tracks the current window's live tab baseline only; saving through the manager UI no longer surfaces that button unless the open tabs themselves changed, and already-saved tabs no longer get pushed back through the runtime inference path just because their saved definition was edited.
- Workspace definitions no longer load or save the legacy `%USERPROFILE%\.wt\workspaces.yaml` format; only the per-folder `workspace.yaml` / `tab.yaml` layout is recognized now, and saving still removes any stale legacy file.
- Workspace window state no longer writes a root-level `workspace-window-state-*.yaml`; each saved workspace now keeps its own `state.yaml`, and empty/non-workspace windows drop their persisted workspace-open marker instead of writing a root state file.
- Workspace and tab names now auto-sanitize to Windows-safe directory names before save/rename, so default/generated names with characters like `:` no longer block persistence.
- Workspace and tab identity/display now come from their folder names only; `workspace.yaml` and `tab.yaml` no longer persist separate `name` or `id` fields, so on-disk rename state cannot drift from the directory layout.
- Workspace chat/draft/terminal artifacts no longer create parallel `.wt\workspaces\ws-{guid}` folders; saved workspaces now reuse the real `%USERPROFILE%\.wt\workspace-name` directory, unsaved windows go under `%USERPROFILE%\.wt\cache\...`, and legacy `.wt\workspaces\...` artifacts are migrated on access.
- Workspace diagnostic logging now writes `.wt\logs\workspace-chat-diagnostics.jsonl`, capturing workspace chat submit/dispatch, TermControl string-send, input-flush, and workspace lock-toggle events for later repro analysis without changing the actual send path.
2026-07-09
- Workspace chat submit now pastes the message body, trims only trailing editor line breaks from that pasted body, and sends exactly one final Enter separately, so Copilot-style bracketed-paste terminals do not see doubled submit carriage returns.
- Workspace chat submit now checks `BracketedPasteEnabled` together with the workspace runtime command, and for Copilot-backed tabs sends single-line prompts via `SendInput` before the final Enter, so the Copilot CLI path does not swallow the submit keystroke after a paste-style send.
- Workspace chat submit now appends the submit `\r` into the same `SendInput` payload for Copilot-backed single-line sends, so Copilot-style CLIs receive the actual execution newline instead of only the message body.
- Workspace chat submit now uses `BracketedPasteEnabled` only for the message body transport (`PasteText` vs `SendInput`), and the final submit is a real Enter key via `RawWriteChar('\r', ...)` instead of another text payload.
- Workspace flyout now exposes three chat submit transport modes for live repro: `PasteText` + separate `\r`, `SendInput` + separate `\r`, and single `SendInput` with inline `\r`, defaulting back to the older paste-plus-separate-`\r` behavior.
- Workspace chat submit now removes the `SendInput` submit variants, always sends `PasteText(text + "\r")`, and shows the active window's `BracketedPasteEnabled` status directly in the workspace flyout.
- Workspace chat submit now pastes only the message body and then emits a real Enter keydown/keyup pair, so submit is no longer encoded as pasted `\r` text.
- Workspace chat submit now delays that real Enter slightly after `PasteText`, matching the old successful “paste first, then submit” timing more closely for bracketed-paste CLIs.
- Workspace chat submit test path now sends the body via `PasteText`, then `SendInput("\r")`, then a delayed real Enter keydown/keyup, matching the latest repro sequence under investigation.
- Workspace chat submit test path now removes `SendInput` again and, after `PasteText`, emits 5 real Enter keydown/keyup pairs spaced 200ms apart for brute-force bracketed-paste submit repro.
- Workspace chat submit test path now injects repeated Enter as actual keydown/keyup events through `RawWriteKeyEvent(VK_RETURN, 0x1c, …)`, instead of routing through `OnDirectKeyEvent` or `RawWriteChar`.
- Workspace chat submit now uses a single window-level OS `SendInput` Enter after refocusing the terminal, instead of injecting keys only into the terminal core/backend path.
- Workspace node lookup helpers now route through `ext\src\core\workspace`, including node-id lookup, visible-node ordinal lookup, visible startup node-id/input-panel queue generation, and persisted open-workspace window-id lookup; glue now only gathers live tabs/runtime state before calling those helpers.
- Workspace open planning now routes through `ext\src\core\workspace::PrepareWorkspaceForOpen(...)`; glue still builds startup actions and performs confirm-dialog, dispatcher, action dispatch, and focus/UI effects.
- Workspace capture assembly and save-side visible node-id rebinding now route through `ext\src\core\workspace` helpers, so glue only gathers live tab/runtime capture data and applies the returned visible node-id order back onto live controls.
- Workspace editor/terminal mutating manager transactions now route through `ext\src\core\workspace` helpers for rename, lock-toggle, node input-panel persistence, workspace removal, node removal, and workspace-name finalization.
- Original `AppLogic` and `TerminalWindow` workspace persistence entry points now bridge through `ext\src\glue\workspace\WorkspaceHostBridge.h` instead of calling `WorkspaceManager` / `WorkspaceStateManager` directly.
- Workspace editor navigation selection encoding/recalculation now routes through `ext\src\core\workspace`, including workspace/node nav tags plus post-delete selection fallback for removed workspaces and nodes.
- Residual workspace read-side `Load()` aggregation now also funnels through `ext\src\glue\workspace\WorkspaceHostBridge.h` helpers for current-state, flyout-state, startup-state, workspace-definition, and persisted window-state refresh.
- Workspace chat submit routing now builds its dispatch plan in `ext\src\core\chat`, so glue keeps only terminal-specific execution and diagnostics while payload/transport policy lives in the pure core helpers.
- Original host workspace change watching now also enters through `ext\src\glue\workspace\WorkspaceHostBridge.h`, so `AppLogic` no longer reaches directly into `WorkspaceManager::DefaultPath()`.
- Workspace open-state loading, open-window lookup, and locked-state persistence now also funnel through `ext\src\glue\workspace\WorkspaceHostBridge.h`, trimming more direct manager/state loading out of `WorkspaceTerminalGlue.cpp`.
- `WorkspaceSaveRuntimeGlue.cpp` now also routes persisted workspace-definition resolution, node input-panel visibility persistence, node-order persistence, and save-baseline persisted-definition lookup through `ext\src\glue\workspace\WorkspaceHostBridge.h`, reducing another batch of mixed runtime/persistence `Load()` calls in glue.
- `WorkspaceSaveRuntimeGlue.cpp` now also routes persisted workspace rename through `ext\src\glue\workspace\WorkspaceHostBridge.h`, so the rename transaction no longer inlines `Load + RenameWorkspace + Save` in glue.
- `WorkspaceEditorGlue.cpp::_SaveWorkspaceEditorState()` now routes its persisted-manager load, current-workspace id remap, and last-opened-workspace validity check through `ext\src\core\workspace::PrepareWorkspaceEditorForSave(...)` and `ext\src\glue\workspace\WorkspaceHostBridge.h`, shrinking another editor save transaction in glue.
- `WorkspaceEditorGlue.cpp` now also routes workspace-definition deletion and node-deletion transaction planning through `ext\src\core\workspace::{PrepareWorkspaceDefinitionRemoval,PrepareWorkspaceNodeRemoval}`, so glue only picks the manager source, surfaces save failures, and applies the returned UI/window effects.
- `WorkspaceTerminalGlue.cpp::_OpenWorkspace()` now also routes its runtime execution branching through `ext\src\core\workspace::ResolveWorkspaceOpenExecutionPlan(...)`, leaving only confirm-dialogs, app-state writes, action dispatch, focus, and tab removal on the host side.
- `WorkspaceSaveRuntimeGlue.cpp` now also routes live-tab capture-node construction and tab-to-workspace matching through `ext\src\core\workspace::{BuildWorkspaceCapturedNode,ResolveWorkspaceBackedTabIndex,ResolveWorkspaceBackedTabNode,FindWorkspaceBackedTabSnapshotIndex}`, so glue just snapshots live host tab/runtime state before calling core.
- `WorkspaceChatTerminalGlue.cpp::_FlushTerminalInputBuffer()` now routes input normalization, multiline commandline fallback, and line splitting through `ext\src\core\chat::BuildTerminalInputFlushPlan(...)`, leaving terminal-specific diagnostics, buffer reads, and dispatch timing in glue.
- `WorkspaceChatTerminalGlue.cpp` now also routes pending output-capture dedup/upsert and output-summary state transitions through `ext\src\core\chat`, via `UpsertPendingTerminalOutputCapture(...)` and `BuildTerminalOutputCaptureResult(...)`, so glue no longer hand-maintains that capture queue/update logic inline.
- `WorkspaceChatTerminalGlue.cpp` now also routes key/char pending-input accumulation, pending-input consume/clear, and two-phase submit preference updates through `ext\src\core\chat`, via `HandleTerminalKeyDown(...)`, `HandleTerminalCharInput(...)`, `ConsumeTerminalPendingInput(...)`, and `UpdateTwoPhaseSubmitPreference(...)`; the leftover working-directory wrapper in glue was removed.
- `TerminalCaptureState` is now defined by `ext\src\core\chat\WorkspaceChatStateHelpers.h` and only re-exported from glue, while `WorkspaceLaunchRuntimeGlue.cpp` now recovers startup action text through `ext\src\core\chat::ResolveCapturedStartupAction(...)` instead of directly encoding the fallback order against capture-state fields in glue.
- `WorkspaceChatTerminalGlue.cpp` and `WorkspaceLaunchRuntimeGlue.cpp` now also route chat-capture state map access and residual field reads through `ext\src\core\chat` helpers (`GetOrCreateTerminalCaptureState(...)`, `FindTerminalCaptureState(...)`, `SeedCapturedShellMetadata(...)`, `ResolveCapturedWorkingDirectory(...)`, `ResolveCapturedOperatingSystem(...)`, `ResolveCapturedShellType(...)`, and the `TerminalCaptureState` overload of `ResolveCapturedStartupAction(...)`) instead of open-coding map/index/find + field fallback sequences in glue.
- `IWorkspaceTerminalPageExtension` no longer exposes the raw `WorkspaceChatTerminalStates()` map; it now exposes narrow `GetOrCreateWorkspaceChatTerminalState(...)` / `FindWorkspaceChatTerminalState(...)` accessors, and both glue plus `LocalTests_TerminalApp\TabTests.cpp` were updated to stop indexing the map directly.
- `IWorkspaceTerminalPageExtension` no longer exposes the raw pending output-capture vector either; it now exposes `UpsertWorkspaceChatPendingOutputCapture(...)`, `TakeReadyWorkspaceChatPendingOutputCaptures(...)`, and `HasWorkspaceChatPendingOutputCaptures()`, while `WorkspaceChatTerminalGlue.cpp` and `LocalTests_TerminalApp\TabTests.cpp` also moved their remaining direct capture-state field writes behind `ext\src\core\chat::SetCapturedLastSubmittedInput(...)` / `SeedCapturedInputState(...)`.
- `WorkspaceChatTerminalGlue.cpp` now also routes the remaining direct capture-state bookkeeping through `ext\src\core\chat` helpers: initial buffer snapshot setup uses `EnsureCapturedBufferSnapshot(...)`, per-line state advance uses `CaptureTrackedTerminalInput(...)`, and command/working-directory reads for submit/output logging use `ResolveCapturedCommand(...)` / `ResolveCapturedWorkingDirectory(...)`.
- Workspace read-side state helpers now route through `ext\src\core\workspace`, including current-workspace display/tab-row/color/locked resolution, workspace editor selection restoration, startup pending-node queue preparation, and flyout open-state view construction.
- Workspace live-tab fallback resolution now routes through `ext\src\core\workspace`, including current-workspace definition resolution plus runtime-node-id / visible-ordinal mapping back to persisted workspace nodes.
- Workspace SSH startup fallback planning now routes through `ext\src\core\workspace`, including current-workspace node resolution and deferred SSH startup input planning; glue now only gathers live profile/runtime metadata and applies the returned runtime state to the host extension.
- Workspace chat submit now branches by `BracketedPasteEnabled`: bracketed-paste tabs keep the paste-plus-5x-window-level-Enter fallback and then restore focus to the workspace input box, while non-bracketed tabs use one `SendInput(text + "\r")`.
- Workspace chat submit diagnostics now log focused-element probes before/after shifting focus to the terminal surface, before each OS-level Enter injection, and after restoring focus back to the workspace input box.
- Workspace chat submit for bracketed-paste tabs now pastes the body, injects Enter through `TermControl::OnDirectKeyEvent` as the primary submit path, and only falls back to window-level OS `SendInput` when that control-level key injection reports failure.
- Workspace chat submit OS fallback now injects Enter as scan-code-only `SendInput` (`KEYEVENTF_SCANCODE`) instead of a virtual-key Enter, so the fallback path more closely matches a real physical keyboard Enter for front-end terminals.
- Workspace chat submit fallback now targets the active WinUI TSF input window first (falling back to the terminal owning hwnd), and logs which hwnd source received the scan-code Enter injection.
- Workspace chat submit fallback now prefers synchronous `WM_KEYDOWN`/`WM_CHAR`/`WM_KEYUP` delivery to the current Win32 focus hwnd, logs that hwnd's window class, and only falls back to scan-code `SendInput` when no focus hwnd is available.
- Workspace chat submit fallback now posts `WM_KEYDOWN`/`WM_KEYUP` into the current Win32 focus hwnd's queue instead of using synchronous `SendMessage`, so the top-level `GetMessage`/`TranslateMessage` loop can generate the normal Enter character path before focus is restored.
- Workspace chat submit now simulates bracketed paste for interactive CLI tabs by sending `ESC[200~ + body + ESC[201~ + "\r\n"` in one `SendInput` payload, so the repro path keeps paste semantics while attaching a trailing `CRLF` directly to the same injected transport.
- Workspace chat submit no longer chooses that two-phase path from `BracketedPasteEnabled` alone; it now reuses each tab's own captured/runtime command state (`LastCommand`, startup action, explicit commandline) to keep Copilot/Claude-style tabs on the interactive CLI submit path even when a newly added tab starts with a different bracketed-paste state.
- Workspace chat submit now latches that interactive-CLI decision per tab once a Copilot/Claude-style command is detected, so later prompt texts like `aa`/`dd` do not overwrite `LastCommand` and accidentally drop the tab back to the plain inline-submit path mid-session.
- Workspace chat submit test path now converts embedded newlines to literal `<br>`, sends the result as plain text through `SendInput`, and appends exactly one trailing `\r`, to test whether Copilot-style CLIs accept a non-paste single-payload submit more reliably than bracketed-paste-wrapped input.
- Workspace flyout now exposes a chat submit transport option with two live repro modes: `SendInput` inline `\r`, or pure focused window-keyboard input for both text and Enter; the window-keyboard mode explicitly shifts focus to the terminal surface first and restores focus back to the workspace input afterward, without using `TermControl::SendInput` for the text body.
- Workspace chat window-keyboard mode now refuses to send while the XAML-focused element is still inside the workspace chat input, repeatedly re-focusing the terminal control for a short window first and logging an explicit abort instead of falling back to another transport.
- Workspace chat capture state no longer keys off the transient TermControl `contentId`; it now follows a stable tab-pane routing key across control-instance churn, diagnostics label the transient id as `controlInstanceId`, and unnamed XAML focus targets log as `(unnamed-framework-element)` instead of looking like renamed UI elements.
- Workspace definition/state loading now accepts only canonical `%USERPROFILE%\.wt\workspaces\<workspace>\workspace.yaml` entries; legacy root-level `.wt\<workspace>` directories no longer participate in load, so split or invalid layouts cannot silently load tabs from the wrong place.
- Workspace chat window-keyboard mode now releases any pressed Ctrl/Shift/Alt/Win modifiers before injecting terminal text and Enter, so Ctrl+Enter submit does not leak a stale Ctrl chord into the terminal and accidentally trigger actions like the default `Ctrl+,` settings shortcut.
- Workspace chat window-keyboard mode now waits briefly after keyboard text injection before sending Enter, so later Copilot tabs do not receive the submit key in the same burst before their prompt has accepted the typed text.
- Workspace chat window-keyboard diagnostics now log the target control's owning hwnd plus Win32 `hwndActive`/`hwndFocus`/`hwndCaret` before ready/send and after send, so later-tab failures can be compared against the actual foreground input sink instead of XAML focus alone.
- Workspace chat window-keyboard mode now explicitly re-activates the terminal owning/hosting window before refocusing the `TermControl`, and diagnostics record whether that window-activation step actually succeeded.
- Workspace chat window-keyboard diagnostics now also record whether `TermControl.Focus(FocusState::Programmatic)` returned success and whether `FocusManager::TryFocusAsync(...)` actually completed with a successful focus move.
- Workspace chat window-keyboard text injection no longer uses Unicode `SendInput`; it now emits layout-mapped physical key scan-code sequences per character and logs any character that cannot be expressed as a real keyboard key in the current layout.
- Added `ext\src\chat\CopilotTerminalSimulator.js`, a standalone Node raw-mode CLI harness that enables bracketed paste, echoes typed-vs-pasted submit behavior, and appends JSONL diagnostics to `%USERPROFILE%\.wt\logs\copilot-terminal-simulator.jsonl` for side-by-side comparison with workspace chat repros.
- The workspace flyout no longer exposes bracketed-paste status or chat submit transport toggles; those live repro controls were removed from the menu.
- Reworked the top-level `CMakeLists.txt` so the CMake portable path now builds from the real `build` directory, drives the VC/MSBuild project graph directly, and uses `ext\src\portable\PortablePackageTool` for packaging instead of wrapping the old PowerShell portable script.
- Portable single-file packaging now compiles the launcher to a temp file first and only then copies it into `bin`, avoiding rebuild failures when the final output path cannot be written in-place during packaging.
- Added `ext\src\portable\PortablePackageTool`, a .NET packager that finds the built Cascadia msix/xaml appx, merges PRI resources, emits the single-file launcher, and writes the final `bin` portable artifacts without going through the old PowerShell packaging entrypoint.
- CMake now normalizes all MSBuild-facing portable paths to native Windows `\` form before invoking the project graph, avoiding mixed-separator `OpenConsoleDir`/`SolutionDir`/package-root arguments that broke the WAP entry-point resolution path.
- The CMake portable MSBuild path now also runs from the repository root with the same dotnet-on-PATH environment and native project paths as the legacy script flow, reducing environment drift between the two build entries.
- `CascadiaPackage.wapproj` now resolves its `EntryPointProjectUniqueName` through `$(OpenConsoleDir)` when that root is provided, so DesktopBridge sees the same absolute WindowsTerminal project identity as the package project's `ProjectReference` list during portable builds.
- Workspace chat diagnostic helpers for optional text fields, hwnd/class capture, GUI-thread focus capture, window activation, and bracketed-submit text normalization are now owned by `ext\src\chat\WorkspaceDiagnosticLog.*`, leaving `TerminalPage.cpp` with thinner UI/event glue.
- Workspace persistence path ownership is now split so `.wt` root resolution, persisted file-name constants, and workspace/tab directory-name sanitization live in `ext\src\workspace\WorkspacePersistencePaths.h`, while `Workspace.cpp` keeps the model serialization and directory traversal glue.
- `microsoft\src\cascadia\TerminalSettingsModel\Workspace.cpp` is now reduced to a compatibility wrapper that includes `ext\src\workspace\WorkspaceModel.cpp`, so the workspace feature's main implementation body lives under `ext\src\workspace` while existing include/project entry points stay stable.
- Workspace metadata/state YAML parsing, serialization, and on-disk workspace directory enumeration are now owned by `ext\src\workspace\WorkspacePersistenceSerialization.cpp`, further shrinking `WorkspaceModel.cpp` toward a thin workspace-model façade.
- Workspace profile-color resolution plus SSH/startup command assembly logic are now owned by `ext\src\workspace\WorkspaceStartupColorLogic.cpp`, so `WorkspaceModel.cpp` keeps less branch-specific rule code inline.
- `microsoft\src\cascadia\TerminalSettingsModel\Workspace.h` is now a compatibility include of `ext\src\workspace\WorkspaceApi.h`, moving the workspace public API surface under `ext\src\workspace` while preserving existing include paths.
- `ext\src\workspace\WorkspaceModel.cpp` is now mostly a composition layer that stitches together `WorkspaceUtilityHelpers.cpp`, `WorkspacePersistenceSerialization.cpp`, `WorkspacePersistenceManagers.cpp`, `WorkspaceStartupColorLogic.cpp`, and `WorkspaceFacadeMethods.cpp`, leaving very little inline branch logic in the top-level workspace model file.
- Workspace chat text trimming, terminal-input normalization, output summarization, trailing-line-break counting, and interactive CLI command detection are now owned by `ext\src\chat\WorkspaceChatTextHelpers.*`, further shrinking `TerminalPage.cpp` toward UI/event glue.
- Workspace chat workspace-key/draft-key/state-key generation, captured-working-directory sync, and two-phase-submit preference decisions are now owned by `ext\src\chat\WorkspaceChatStateHelpers.*`, leaving `TerminalPage.cpp` with less custom state-rule logic.
- A contiguous block of workspace chat method bodies from `TerminalPage.cpp` now lives in `ext\src\chat\WorkspaceChatTerminalGlue.cpp` and is included back as a thin bridge, so more of the workspace chat implementation sits under `ext\src\chat` instead of inline in the forked file.
- A contiguous block of workspace flyout/open-workspace/editor-selection/lock-state/tab-row methods from `TerminalPage.cpp` now lives in `ext\src\workspace\WorkspaceTerminalGlue.cpp` and is included back as a thin bridge, moving more workspace UI/interaction logic under `ext\src\workspace`.
- A second contiguous `TerminalPage.cpp` workspace/UI state block now lives in `ext\src\workspace\WorkspaceUiStateGlue.cpp`, covering workspace chat panel initialization, clip/header refresh, pending workspace-node state transfer, tab-surface focus, title-policy application, and chat draft reload behind a thin include bridge.
- A contiguous `TerminalPage.cpp` workspace chat dispatch block now lives in `ext\src\chat\WorkspaceChatDispatchGlue.cpp`, covering dispatch transport selection, keyboard-submit paths, draft persistence, input-height recalculation, submit orchestration, and chat panel collapse state behind a thin include bridge.
- A contiguous `TerminalPage.cpp` workspace save/runtime block now lives in `ext\src\workspace\WorkspaceSaveRuntimeGlue.cpp`, covering workspace rename UI, current-workspace capture, node/runtime-state lookup, input-visibility persistence, live tab-order persistence, and dirty-state comparison behind a thin include bridge.
- A contiguous `TerminalPage.cpp` workspace editor block now lives in `ext\src\workspace\WorkspaceEditorGlue.cpp`, covering editor selection accessors, add/delete workspace and node actions, workspace/node removal persistence, editor save, and manager-tab rebuild behind a thin include bridge.
- A contiguous `TerminalPage.cpp` workspace saver/baseline block now lives in `ext\src\workspace\WorkspaceSaverGlue.cpp`, covering the workspace save TeachingTip UI, suggested-name generation, persisted-name normalization, current-workspace save baseline refresh, and save-to-workspace persistence behind a thin include bridge.
- The large `TerminalPage.cpp` workspace manager UI builder now lives in `ext\src\workspace\WorkspaceManagerContentGlue.cpp`, moving the NavigationView tree, behavior/general/node editor construction, and footer action wiring behind a thin include bridge.
- Additional workspace exit/save handlers now live with `WorkspaceSaverGlue.cpp`, and workspace startup/runtime-state methods now live in `ext\src\workspace\WorkspaceLaunchRuntimeGlue.cpp`, moving confirm-save-on-exit, saver key handling, workspace-window-state persistence, startup-action capture, and runtime metadata resolution behind thin include bridges.
- `TerminalPage.h` workspace-specific enums and runtime/capture state structs now live in `ext\src\workspace\TerminalPageWorkspaceTypes.h`, while `TerminalPage` keeps thin aliases so existing call sites and tests stay stable.
- Workspace-specific `Create()` tab-row wiring and startup workspace preloading now live in `ext\src\workspace\WorkspaceCreateGlue.cpp`, moving the workspace save/name button hookup, workspace-name timer wiring, and `_startupWorkspaceId` pre-processing behind thin bridges.
- The remaining large workspace-specific anonymous-namespace helper block from `TerminalPage.cpp` now lives in `ext\src\workspace\WorkspaceTerminalPageHelpers.cpp`, moving runtime metadata inference, SSH startup replay helpers, color helpers, node-lookup helpers, and workspace-layout comparison behind an include bridge.
- The last explicit workspace method left in `TerminalPage.cpp`, `_BuildWorkspaceNodeArgs()`, now lives in `ext\src\workspace\WorkspaceSaveRuntimeGlue.cpp`, leaving the forked cpp with no remaining `TerminalPage::*Workspace*` implementation bodies.
- Terminal and tab event hookup methods now live in `ext\src\workspace\WorkspaceTerminalEventsGlue.cpp`, moving the workspace/chat-specific TermControl string/key capture wiring and `ShowWorkspaceInputPanel` tab-property glue behind another include bridge.
- The remaining top-level workspace/chat helper block from `TerminalPage.cpp` now lives in `ext\src\chat\WorkspaceChatTopLevelHelpers.cpp`, moving chat submit timing/constants, diagnostic logging, focused-element probes, keyboard text/Enter injection, and modifier-release helpers behind an ext include bridge.
- The workspace startup replay portion of `TerminalPage::_ConnectionStateChangedHandler()` now lives in `ext\src\workspace\WorkspaceLaunchRuntimeGlue.cpp` as `_ReplayPendingWorkspaceStartupInput()`, leaving the forked connection-state handler thinner and pushing more runtime replay logic into ext-owned glue.
- The remaining workspace-specific startup-action queue cleanup and skip-next-SendInput branch from `TerminalPage::ProcessStartupActions()` now live in `ext\src\workspace\WorkspaceCreateGlue.cpp`, further trimming the forked startup-processing path to a thinner orchestration loop.
- The tab drag/drop block from `TerminalPage.cpp` now lives in `ext\src\workspace\WorkspaceTabDragGlue.cpp`, moving the workspace-lock-aware drag-start, drag-over, drop, dropped-outside, and send-to-other-window glue behind another ext include bridge.
- Remaining one-off workspace checks in `TerminalPage.cpp` now route through ext-owned helpers: split blocking for settings/workspace-manager tabs and settings-reload workspace surface refresh live in `ext\src\workspace\WorkspaceTerminalGlue.cpp`, while the `CurrentWorkspaceId()` getter now lives in `ext\src\workspace\WorkspaceLaunchRuntimeGlue.cpp`.
- The workspace save-confirmation branch at the start of `TerminalPage::CloseWindow()` now lives in `ext\src\workspace\WorkspaceSaverGlue.cpp` as `_ConfirmWorkspaceCloseWindowIfNeeded()`, further trimming direct workspace exit-flow logic from the forked file.
- `TerminalPage.h` now pulls its workspace/chat member block plus the bulk workspace/chat declaration surfaces from `ext\src\workspace\TerminalPageWorkspaceMembers.h`, `TerminalPageWorkspaceMethods.h`, and `TerminalPageWorkspaceRuntimeMethods.h`, shrinking the forked header to thin include slots while leaving symbol names unchanged.
- `TerminalPage.h` now also routes the remaining workspace public API declarations, early workspace members, startup/saver workspace members, and tail workspace declarations through `TerminalPageWorkspacePublicMethods.h`, `TerminalPageWorkspaceEarlyMembers.h`, `TerminalPageWorkspaceStartupMembers.h`, and `TerminalPageWorkspaceTailMethods.h`, leaving even less branch-specific surface inline in the forked header.
- Remaining workspace runtime declarations for startup-action/directory/OS/shell resolution and workspace node load/color application now live in `TerminalPageWorkspaceRuntimeMethods.h`, while `TerminalPage.cpp` now uses ext-owned helpers for conditional workspace runtime-state registration during pane creation and for the workspace-aware middle-click close-button hook policy.
- The remaining `_updateAllTabCloseButtons()` implementation now lives in `ext\src\workspace\WorkspaceTerminalGlue.cpp`, so the forked `TerminalPage.cpp` no longer keeps that workspace-aware close-button policy body inline.
- The remaining top-of-file workspace/chat include surface for `TerminalPage` is now funneled through `ext\src\workspace\TerminalPageWorkspaceIncludes.h`, and redundant cpp-level includes for workspace model/manager/chat types were dropped from `TerminalPage.cpp`.
- `TerminalPage.cpp` now also collapses its workspace/chat cpp-bridge stack through `ext\src\workspace\TerminalPageWorkspacePrelude.cpp` and `TerminalPageWorkspaceGlue.cpp`, so the forked file keeps a much smaller anonymous-namespace prelude and implementation include surface.
- `TerminalPage.h` now also collapses its remaining workspace/chat public aliases plus private member/method include slots through `TerminalPageWorkspacePublicSurface.h`, `TerminalPageWorkspacePrivateMembersSurface.h`, and `TerminalPageWorkspacePrivateMethodsSurface.h`, so the forked header keeps fewer scattered ext-include seams.
- `TerminalPage.cpp` now also funnels its remaining top-of-file workspace/chat helper headers through `ext\src\workspace\TerminalPageWorkspaceCppIncludes.h`, leaving the forked cpp with one smaller ext include seam before the prelude/glue bridges.
- `TerminalPage.h` now also routes the remaining `_RegisterTerminalEvents()`, `_RegisterTabEvents()`, and `_updateAllTabCloseButtons()` workspace/chat declarations through `TerminalPageWorkspaceMethods.h`, leaving fewer standalone branch-specific method declarations inline in the forked header.
- `TerminalPage.cpp` now also moves the full `Create()`, `ProcessStartupActions()`, and `CloseWindow()` method bodies into `ext\src\workspace\WorkspaceLifecycleGlue.cpp`, so the forked cpp keeps less lifecycle/startup/close implementation inline and more of that surface lives behind the ext glue aggregator.
- `TerminalPage.cpp` now also moves the full `_SplitPane()`, `_RefreshUIForSettingsReload()`, and `_ConnectionStateChangedHandler()` method bodies into `ext\src\workspace\WorkspaceLifecycleGlue.cpp`, further shrinking the forked file's remaining split/refresh/connection-state implementation down to the ext lifecycle glue layer.
- `TerminalPage.cpp` now also moves `_MakeTerminalPane()` and `_MakePane()` into `ext\src\workspace\WorkspaceLifecycleGlue.cpp`, so the forked file no longer keeps the remaining workspace-aware pane-creation path inline.
- `TerminalPage.cpp` now also moves `CreateTabFromConnection()` and `_CompleteInitialization()` into `ext\src\workspace\WorkspaceLifecycleGlue.cpp`, further shrinking the forked startup/lifecycle block around first-layout initialization and initial tab creation.
- `TerminalPage.cpp` now also moves the dialog/new-tab-flyout block (`_ShowAboutDialog()`, `_ShowDialogHelper()`, confirm/paste dialogs, `_CreateNewTabFlyout*`, `_OpenNewTabDropdown()`, `_OpenNewTerminalViaDropdown()`) into `ext\src\workspace\TerminalPageNewTabFlyoutGlue.cpp`, further shrinking the forked UI/menu surface into ext-owned glue.
- `TerminalPage.cpp` now also moves `RequestQuit()`, `PersistState()`, `_ShouldWarnOnClose()`, `_ShouldWarnOnCloseTab()`, and `Panes()` into `ext\src\workspace\WorkspaceLifecycleGlue.cpp`, further shrinking the forked window-lifecycle/persist/close-warning block.
- `TerminalPage.cpp` now also moves `_Scroll()`, `_MovePane()`, `_DetachPaneFromWindow()`, `_DetachTabFromWindow()`, `_MoveContent()`, `_MoveTab()`, `_activePaneChanged()`, and `AttachContent()` into `ext\src\workspace\WorkspaceLifecycleGlue.cpp`, further shrinking the forked pane/tab move and attach block.
- `TerminalPage.cpp` now also moves `_ToggleSplitOrientation()`, `_ResizePane()`, `_ScrollPage()`, and `_ScrollToBufferEdge()` into `ext\src\workspace\WorkspaceLifecycleGlue.cpp`, finishing off the adjacent split/resize/scroll helper block in ext-owned glue.
- `TerminalPage.cpp` now also moves the adjacent URI/open-link and terminal notice dialog block (`_OpenHyperlinkHandler()`, `_ShowCouldNotOpenDialog()`, `_IsUriSupported()`, `_IsUriConsideredSomewhatSafe()`, `_ControlNoticeRaisedHandler()`, `_ShowControlNoticeDialog()`) into `ext\src\workspace\TerminalPageInteractionGlue.cpp`.
- `TerminalPage.cpp` now also moves `_OnTabCloseRequested()`, `_CreateNewControlAndContent()`, `_AttachControlToContent()`, `_SetupControl()`, `_restartPaneConnection()`, `_SetBackgroundImage()`, `SetStartupConnection()`, `DialogPresenter()`, and `TaskbarState()` into `ext\src\workspace\TerminalPageControlGlue.cpp`.
- `TerminalPage.cpp` now also moves `TitlebarClicked()`, `WindowVisibilityChanged()`, `_Find()`, `ToggleFocusMode()`, `SetFocusMode()`, `ToggleFullscreen()`, and `ToggleAlwaysOnTop()` into `ext\src\workspace\TerminalPageWindowStateGlue.cpp`.
- `TerminalPage.cpp` now also moves `_FocusActiveControl()`, `FocusMode()`, `Fullscreen()`, `AlwaysOnTop()`, `ShowTabsFullscreen()`, `SetShowTabsFullscreen()`, `SetFullscreen()`, `Maximized()`, `RequestSetMaximized()`, `_makeSettingsContent()`, and `OpenSettingsUI()` into `ext\src\workspace\TerminalPageSettingsUiGlue.cpp`.
- `TerminalPage.cpp` now also moves `_GetTabImpl()`, `_ComputeScrollDelta()`, and `_ReadSystemRowsToScroll()` into `ext\src\workspace\TerminalPageHelpersGlue.cpp`, plus `ShowKeyboardServiceWarning()`, `KeyboardServiceDisabledText()`, `_UpdateTeachingTipTheme()`, `IdentifyWindow()`, and `ShowTerminalWorkingDirectory()` into `ext\src\workspace\TerminalPageTeachingTipGlue.cpp`.
- `TerminalPage.cpp` now also moves `_WindowRenamerActionClick()`, `_RequestWindowRename()`, `_WindowRenamerKeyDown()`, `_WindowRenamerKeyUp()`, `GetClosestProfileForDuplicationOfProfile()`, `_OpenElevatedWT()`, `_maybeElevate()`, `_CloseOnExitInfoDismissHandler()`, `_KeyboardServiceWarningInfoDismissHandler()`, `_IsMessageDismissed()`, and `_DismissMessage()` into `ext\src\workspace\TerminalPageWindowActionsGlue.cpp`.
- `TerminalPage.cpp` now also moves `_updateThemeColors()`, `_updatePaneResources()`, `_adjustProcessPriority()`, and `WindowActivated()` into `ext\src\workspace\TerminalPageThemeRuntimeGlue.cpp`.
- `TerminalPage.cpp` now also moves `_ControlCompletionsChangedHandler()`, `_OpenSuggestions()`, `_PopulateContextMenu()`, `_PopulateQuickFixMenu()`, `_windowPropertyChanged()`, and `_CreateRunAsAdminFlyout()` into `ext\src\workspace\TerminalPageSuggestionsGlue.cpp`.

2026-07-17
- `WorkspaceExtension`'s CMake include path now pulls the generated WinRT projection roots in the same order the host build expects, including the `WindowsTerminal\Generated Files\winrt` projections first, so ext/full portable builds stop failing on missing `Microsoft.UI.Xaml.Controls.h` / `Microsoft.Terminal.Settings.Editor.h` and the `Settings.Model` runtimeclass inline constructors resolve correctly during link.
- Added a `UnitTests_SettingsModel` regression case that ignores workspace directories lacking `workspace.yaml`, covering the `C:\Users\Administrator\.wt\workspaces\ceshi` shape where only tab-local chat artifacts exist and ensuring that layout does not load as a saved workspace again.
- Rebuilt both `ext` and `full`, and the final `bin\WindowsTerminalPortableGeekEdition_System_0.0.1.0_x64.exe` now launches through a fresh `%LOCALAPPDATA%\WTP\...` extraction path without reproducing the earlier immediate crash/exit during this verification pass.
